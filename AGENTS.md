# Threads Media Manager

이 프로젝트는 threads-bot·threads-web과 독립된 로컬 제품이다. [구현 계획](docs/implementation-plan.md)을 먼저 확인한다. 최신 기준은 **플러그인은 수집 완료까지, 다운로드와 관리는 로컬 Electron 앱**이다.

- 플러그인 소스는 `plugins/threads-collector/`. 초기 설정·계정 목록·로그인된 Codex 내부 브라우저 수집·Excel 원본만 담당한다. 다운로드 코드·다운로드 스킬·SQLite·Pillow·ffprobe 의존성을 넣지 않는다.
- 수집은 제공된 내부 브라우저 도구만 사용한다. 직접 페이지 API·쿠키 추출·별도 HTTP 수집으로 대체하지 않는다.
- 결과 Excel이 원본이다. 다음 수집 기준은 최신 검증된 Excel에서 읽으며 accounts.xlsx의 기준·실행 열은 표시 사본이다. JSONL은 `_work`의 임시 수집 기록이며 결과·계정 Excel 검증 후 정리한다. 영구 JSONL과 별도 보고서를 만들지 않는다.
- `local-runtime/`은 앱이 소유하는 다운로드·상태 코드다. 앱만 state.db와 미디어를 변경하며 원문·서명 URL을 DB에 복제하지 않는다. 다운로드 버튼 한 번으로 고정된 게시글 회차를 순차 처리한다. 목록에는 모든 첨부가 저장된 게시글만 표시한다. 다운로드·재개 전에 확인창을 띄우고, 진행·대기 중에는 다른 조작을 막는 전체 화면 상태 레이어를 보여준다. [최신 다운로드 계약](docs/phase-1e-download.md)을 따른다. 사용자 결정으로 다운로드의 일일 횟수 제한은 없다. 기존 요청 이력·대기·중단 상태는 보존한다. 사용자 중지 버튼은 제공하지 않으며 다운로드 중 일반 앱 종료를 차단한다. 비정상 종료 뒤에는 명시적 이어서 다운로드로 로컬 복구와 대상 재검증을 실행한다. `status`는 읽기 전용이고 `recover`는 로컬 복구만 수행하며 `resume`은 사용자의 명시적 전송 요청이다. 설정의 DB 백업·복원은 [DB 관리 계약](docs/database-maintenance.md)을 따른다.
- 앱 시작·새로고침과 새 다운로드 시작 전 `cleanup-source`는 `source_not_ready`·`attachment_not_ready`인 불완전 수집 글만 `post_deletions`에 기록하여 앱에서 논리적으로 삭제한다. 완전성은 게시글별 캡션·첨부 수집 상태와 실제 첨부·URL 연결로 판단한다. 상위 수집 실행이 중단되거나 `partial`이어도 해당 글이 완전하면 정상 다운로드 대상으로 유지한다. Excel·수집 기준·원본 해시는 변경하지 않으며 원격 요청·파일 삭제도 없다. 기존 `meta.download_exclusions` 중 게시글별 검사로 정상임이 확인된 글은 제외를 해제한다. 여전히 불완전하며 작업 없는 글만 삭제 기록으로 이관한다. jobs·부분 파일·다운로드 파일·AI 생성본·등록·편집 등 기존 작업이 하나라도 있는 글은 원본이 나중에 불완전해져도 보호한다. 일반 상태와 함께 `cleanedPosts`를 반환하고 정리한 글은 목록·미완료 수·후속 계획에서 제외한다. 정상 글의 다운로드 실패·강제 종료를 수집 불완전으로 취급하거나 삭제하지 않으며 기존 파일·계획·요청·대기를 보존하고 명시적으로 재개한다. `round_range` 보류 목록도 정리 대상이 아니다. 현재 수집 스킬이나 설치는 변경하지 않는다. 정리한 불완전 글 수는 `cleanedPosts`, 정상으로 확인해 제외를 해제한 글 수는 `releasedPosts`로 구분한다.
- 수집 탐색 중에는 JSONL만 기록하며 공통 잠금을 잡지 않는다. 수집 종료·정규화 후 `commit-source`를 토큰 없이 호출하고, Excel 최종 반영 구간에서만 기존 공통 잠금을 사용한다. 로컬 앱은 탐색 중 기존 자료로 편집·작성을 계속한다. 실제 HTTP·429·인증·로컬 무결성·원본 변경 등 다운로드 첫 오류는 다운로드 전체 중단이다. 별도로 요청된 수집을 DB 오류로 제어하지 않으며, 새 수집으로 앱의 중단·요청 이력·대기를 초기화하지 않는다.
- 사용자 자료·서명 URL·미디어·세션·DB는 코드와 분리한다. 기존 완료 파일·수집 기준·요청 이력을 보존한다. 사용자가 앱 확인창에서 승인한 삭제는 예외다. 편집본/게시글별 대상 파일을 삭제하고 게시글 삭제 시 Excel에 삭제 여부·시각을 기록하되 수집 기준과 기존 요청·중단·대기는 보존한다. [삭제 계약](docs/media-deletion.md)을 따른다.
- 목록 상단의 `확인할 수집·저장 정보` 영역은 제공하지 않는다. 일부 코드만 숨기는 방식이 아니라 수집·저장 진단 경고 목록과 개수 표시 자체를 제거한다. 사용자는 정상 수집 글을 다운로드하면 되고, 불완전 수집 글은 앱의 정리 정책으로 처리한다.
- 화면은 local-video-manager의 카드 그리드·색상·모달을 참고한다. 상세 모달은 상단 미디어·하단 정보를 배치하며, 여러 첨부는 목록과 상세에서 순서를 유지하는 캐러셀로 보여준다. 로컬 파일만 표시하며 그리드 영상은 자동재생하지 않는다. 상세에서 현재 선택된 영상은 자동재생한다. 기존 앱의 로그인·서비스·DB 연결은 가져오지 않는다.
- 기존 단독 수집 스킬·기존 프로젝트·사용자 파일을 임의 변경·삭제하지 않는다.
- 개발·단계별 테스트는 Mac, 기능은 Mac/Windows 지원. Mac 배포는 없으며 Windows 실제 기능·설치·자동 업데이트 검증은 최종 단계다.
- 패키지 형식·설치·새 대화 로드·실제 기능·Windows 검증을 구분해 보고한다. GitHub 생성·push·Release와 멤버 배포는 대상과 범위를 확정한 뒤 진행한다.

- Phase 2 등록은 한 원글의 원본·편집본(개발 실행에서는 AI 생성본 포함) 안에서 선택·순서·캡션을 관리하며 원글당 한 개를 SQLite에 저장한다. 서로 다른 원글을 섞지 않는다. Codex 캡션 생성은 명시적 버튼 요청에서만 선택 이미지와 원문 캡션을 전달한다. 영상만 선택하면 비활성화하며 영상 프레임을 추출하지 않는다. 원본 캡션의 의미·말투·흐름이 우선이고 이미지는 보조 자료다. 원문·미디어·등록 내용을 자동 변경하거나 실제 Threads 게시를 실행하지 않는다. [등록 계약](docs/phase-2-registration.md)을 따른다.

- API 전 AI 이미지·제품 홍보 초안 시험은 [AI 생성 계약](docs/ai-content-generation.md)을 따른다. AI 이미지 생성은 `app.isPackaged === false`인 개발 실행에서만 제공하며 배포 앱에서는 버튼·AI 후보·생성 및 조회 IPC를 차단한다. 기존 AI 캡션 기능과 구분한다. 개발 작성 화면은 원본→편집본→저장된 AI 생성본 순서로 제공하며 사용자가 직접 선택·등록한다. AI 버전별 미디어 ID를 유지해 재생성이 기존 등록 선택을 바꾸지 않게 한다. 수집 상세의 명시적 AI 생성에서 편집본을 제외한 원본 이미지 전체와 원문 캡션을 전달한다. 제품명 누락은 기본 상황으로 보고 제품군 홍보로 진행한다. 원본은 비율·대략적인 촬영 거리와 구도·피사체 비중·홍보 컨셉만 참조한다. 보이는 얼굴은 원본과 다른 가상 인물로 만들고 의상·배경·포즈도 모두 분명히 바꾼다. 신체 일부·사물 클로즈업에서는 동작이나 배치와 배경을 바꾼다. 이미지 모델이 구체적인 외모·옷·장소·자세를 선택하도록 범용 조건만 전달하며 사용자에게 매번 방향을 요구하지 않는다. 색감·조명·작은 화각 변화만으로는 충분하지 않다. 특정 게시글의 해법을 공통 지침에 고정하지 않으며 이미지 생성용 문구는 핵심 조건만 짧게 전달한다. 인물은 가상 성인 여성만 생성하며 얼굴은 원본 구도에 맞을 때 허용한다. 손 클로즈업에 얼굴·인물 전경을 추가하지 않는다. 결과는 작업 폴더의 별도 AI 초안으로 보관하고 원문·미디어·기존 등록 내용을 덮어쓰거나 자동 등록·게시하지 않는다.
